# =============================================================================
# Hive Server Configuration
# =============================================================================
# Copy this file to .env and customize for your deployment

# -----------------------------------------------------------------------------
# Required Settings
# -----------------------------------------------------------------------------

# JWT secret for signing authentication tokens
# IMPORTANT: Generate a secure secret with: openssl rand -base64 32
# Must be at least 32 characters long
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------

# Port to run the server on
PORT=3000

# -----------------------------------------------------------------------------
# Storage Paths
# -----------------------------------------------------------------------------

# SQLite database for user accounts
DATABASE_PATH=./data/users.db

# Directory for vault file storage
VAULTS_PATH=./data/vaults

# Directory for automatic backups
BACKUPS_PATH=./data/backups

# -----------------------------------------------------------------------------
# Security Settings
# -----------------------------------------------------------------------------

# Allowed origins for CORS (comma-separated)
# Add your domain(s) for production deployment
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: Password required for admin token endpoint
# If set, requests to /api/admin/token must include this password
# ADMIN_PASSWORD=your-secure-admin-password

# -----------------------------------------------------------------------------
# Production Recommendations
# -----------------------------------------------------------------------------
# 
# 1. Use a reverse proxy (nginx, Caddy) with HTTPS/TLS
# 2. Set ALLOWED_ORIGINS to your specific domain(s)
# 3. Generate a strong JWT_SECRET
# 4. Set ADMIN_PASSWORD if exposing the admin endpoint
# 5. Configure firewall to only expose necessary ports
# 6. Set up external backups of the data/ directory
# 7. Monitor server logs for suspicious activity
#
# Example nginx configuration is in the README.md
